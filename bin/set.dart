void main()
{
  //set is unorder collection type
  //1.set has no index number.
  //2.any dublicate value print only one time.
  //3.make a set with {} bareket.

  var myset ={10,20,30,04,0,5,10,67};

  //dynamic myset2={11,2,3,4};

  // Set myset3 = {12,23,123,1,"qe3qwe"};

  // Set<int> myset4={11,23,123,123,4};
  // print(myset.first);
  //print(myset.last);
  //print(myset.single);
  //print(myset.iterator);
  //print(myset.length);
  //print(myset.isEmpty);
  // print(myset.isNotEmpty);
  //print(myset.runtimeType);
  //myset.add(70);
  // myset.addAll([10,56,85]);
  //myset.clear();
  //myset.remove(30);
  //myset.removeWhere((element) => element==5);
  //print(myset.contains(5));
  //print(myset.every((element) => element<50));
  //print(myset.any((element) => element<500));
  var a={10,20,30,55};
  var b={10,20,30,40,55};
  //print(a.intersection(b));
  //print(b.difference(a));
  //print(a.union(b));
  //print(a.lookup(100));
  //print(a.take(3));
  //print(a.skip(2));
  //print(a.skipWhile((value) => value<50));
  //a.retainAll([20,55]);
  //a.retainWhere((element) => element<50);
  //print(a.elementAt(0));
  //print(a);
}
